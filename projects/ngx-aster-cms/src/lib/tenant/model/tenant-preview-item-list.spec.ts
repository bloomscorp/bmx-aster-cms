import { TenantPreviewItemList } from './tenant-preview-item-list';

describe('TenantPreviewItemList', () => {
  it('should create an instance', () => {
    expect(new TenantPreviewItemList()).toBeTruthy();
  });
});
