import { AsterFormInputService } from './aster-form-input-service';

describe('AsterFormInputService', () => {
  it('should create an instance', () => {
    expect(new AsterFormInputService()).toBeTruthy();
  });
});
