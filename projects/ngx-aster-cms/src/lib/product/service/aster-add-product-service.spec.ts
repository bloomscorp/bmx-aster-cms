import { AsterAddProductService } from './aster-add-product-service';

describe('AsterAddProductService', () => {
  it('should create an instance', () => {
    expect(new AsterAddProductService()).toBeTruthy();
  });
});
