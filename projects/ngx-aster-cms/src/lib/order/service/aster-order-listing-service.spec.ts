import {AsterOrderListingService} from './aster-order-listing-service';

describe('AsterOrderListingService', () => {
  it('should create an instance', () => {
    expect(new AsterOrderListingService()).toBeTruthy();
  });
});
