import { AsterRequestMapper } from './aster-request-mapper';

describe('AsterRequestMapper', () => {
  it('should create an instance', () => {
    expect(new AsterRequestMapper()).toBeTruthy();
  });
});
